name: CI Flywheel

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the main branch
on:
  push:
    branches: [ main ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "deploy"
  deploy:
    # It is recommended to not modify any of the following configuration unless you know what you are doing.
    # To update your SFTP configuration, update the previous values under the `env` key.
    name: Deploy

    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v4

      # Deploys to FTP
      - name: SFTP Deploy
        id: upload
        uses: Creepios/sftp-action@v1.0.4
        with:
          host: ${{ secrets.SFTP_HOST }}
          port: 22
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          # The local folder to copy, defaults to root project folder
          localPath: ./
          # This was generated using the output of
          remotePath: '/home/PeninsulaValdes_german/public_html/wp-content/themes/agencias-chubut-patagonia'
          agent: false